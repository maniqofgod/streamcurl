version: '3.8'

services:
  vps_agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streamcurl_vps_agent
    restart: always
    ports:
      - "8001:8001"
    env_file:
      - .env
    networks:
      - agent-net

networks:
  agent-net:
    driver: bridge